<Bindings>
	<!-- window commands -->
	<Binding name="TOGGLEFISHINGBUDDY_OUT">
		if ( keystate == "down" ) then
			FBEnvironment.ToggleFishingBuddyFrame("FishingOutfitFrame");
		end
	</Binding>
</Bindings>